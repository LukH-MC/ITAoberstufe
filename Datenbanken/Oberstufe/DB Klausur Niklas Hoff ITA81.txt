-- Niklas Hoff ITA81 --

1. a) DML
   b) Das Update Table wird zurückgenommen und die Datenbank auf den vorherigen Stand zurückgesetzt
   
2. INSERT INTO emp_copy (employee_id, first_name, last_name, email, Einstellung, Tätigkeit, Manager_Nr, Abteilung)
   SELECT EMPLOYEE_ID, FIRST_NAME, LAST_NAME, EMAIL, HIRE_DATE, JOB_ID, MANAGER_ID, DEPARTMENT_ID FROM employees;

3. INSERT INTO emp_copy
   (
    [employee_id], [first_name], [last_name], [email], [Einstellung], [Tätigkeit], [Manager_Nr], [Abteilung]
   )
   VALUES
   (
    510, 'Hans', 'Müller', 'hanmüller@comp.com', DATE_FORMAT('01-06-2012', '%Y-%M-%d', 'de_DE') , 'AC_ACCOUNT', 102, 60;
   ),
   (
    520, 'Maria', 'Schuster', 'marschuster@comp.com', DATE_FORMAT('01-01-2013', '%Y-%M-%d', 'de_DE') , 'AC_ACCOUNT', 100, 90;
   ),
   (
    530, 'Thomas', 'Freiberger', 'thofreiberger@comp.com', DATE_FORMAT('15-07-2011', '%Y-%M-%d', 'de_DE') , 'AC_ACCOUNT', 100, 50;
   );

4. SELECT * FROM emp_copy
   WHERE employee_id > 500;

5. DELETE FROM emp_copy
   WHERE last_name CONTAINS('u') OR CONTAINS('r');

6. Delete: Tabelle wird wiederhergestellt
   Truncate: Daten in Tabelle werden wiederhergestellt

7. a) Interaktion mit der Datenbank wenn Befehle jeweils nach erfolgreicher Ausführung aufeinander folgen müssen. Sollte das nicht passieren wird keiner der beiden Befehle ausgeführt.
   b) Update muss auf ein erfolgreiches Insert folgen und auch erfolgreich ausgeführt werden

8. 1)
   2)