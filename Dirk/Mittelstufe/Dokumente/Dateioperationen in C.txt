#include <stdio.h>
#include <iostream>
using namespace std;


#define _CRT_SECURE_NO_WARNINGS


void main()
{
	FILE* f;
	int dummy;

	fopen_s(&f, "c:/temp/zahlen.txt", "r");
	if (f == NULL) cout << "Datei konnte nicht geöffnet werden";
	else
	{
		fscanf_s(f, "%d", &dummy);
		cout << dummy << endl;

		fscanf_s(f, "%d", &dummy);
		cout << dummy << endl;

		fscanf_s(f, "%d", &dummy);
		cout << dummy << endl;

		fscanf_s(f, "%d", &dummy);
		cout << dummy << endl;

		fscanf_s(f, "%d", &dummy);
		cout << dummy << endl;

		int dummy2; 

		for (int i = 0; i < dummy; i++)
		{

			fscanf_s(f, "%d", &dummy2);
			cout << dummy2 << "   ";
			fscanf_s(f, "%d", &dummy2);
			cout << dummy2 << endl;

		}
		fclose(f);
	}
}